#include<stdio.h>

void main()
{
int length,width,area;
printf("Enter the length of the rectangle respectively : ");
scanf("%d", &length);
printf("Enter the width of the rectangle respectively : ");
scanf("%d",&width);
area = length*width;
printf("The area of the rectangle is : %d\n",area);


}
